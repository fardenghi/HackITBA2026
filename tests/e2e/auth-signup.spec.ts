import { test } from '@playwright/test';

test.describe('auth signup flow', () => {
  test('desktop and mobile signup journey placeholder', async () => {
    test.fail(true, 'Signup flow not implemented yet.');
  });
});
