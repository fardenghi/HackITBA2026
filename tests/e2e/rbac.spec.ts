import { test } from '@playwright/test';

test.describe('rbac flow', () => {
  test('desktop and mobile role guard placeholder', async () => {
    test.fail(true, 'RBAC flow not implemented yet.');
  });
});
